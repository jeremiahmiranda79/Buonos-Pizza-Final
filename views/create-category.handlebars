<div class="create-category-background">
  <div class="container-create-category-header">
    <center>
      <h1>Create category</h1>
      <h4>For admin only</h4>
    </center>
  </div>

  <div class="container-create-category">
    <div class="container-fluid contact">
      <form id="addCat">
        <div class="">
          
          <h1 class="">Menu Categories</h1>
          <button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#collapsable-catgeory-list" aria-expanded="false" aria-controls="collapsable-catgeory-list">
            See all active categories
          </button>

          <div class="collapse" id="collapsable-catgeory-list">
            <div class="card card-body">
              <ul>
                {{#each cats as |cat|}}  
                  <li>{{cat.name}}</li>           
                {{/each}}
              </ul>
            </div>
          </div>

          <div class="mb-3 py-3">
            <input 
              type="text"
              class="form-control"
              id="name"
              placeholder="Add New Category"/>
            </input>
          </div>
           
          <div class="d-flex justify-content-evenly">
            <button
              type="submit"
              class="btn btn-primary"
              id="addCatButton">
              Submit
            </button>
            <div>
              <a href="/menu" id="cancelButton" class="btn btn-primary">Cancel</a>
            </div>
          </div>  
        </div>
      </form>
    </div>
  </div>
</div>




{{!-- <form id="addCat">
  <div class="">
    
    <h1 class="">Menu Categories</h1>
    <ul>
      {{#each cats as |cat|}}  
        <li>{{cat.name}}</li>           
      {{/each}}
    </ul>
       
    <input 
      type="text"
      class=""
      id="name"
      placeholder="Add New Category"/>
    </input>
    <button
      type="submit"
      class=""
      id="addCatButton">
      Submit
    </button>
    <div>
      <a href="/menu" id="cancelButton" class="">Cancel</a>
    </div>
  </div>
</form> --}}

<script src="/js/createCategory.js"></script>