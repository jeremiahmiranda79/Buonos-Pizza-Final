{{#each items as |c|}}
  <br><br>
  <h1 id="{{c.id}}"><strong>{{c.name}}</strong></h1>
  {{#each c.items_models as |i|}}
    <div class="list-group">
      <a href="menu/{{i.id}}" class="list-group-item list-group-item-action">
        <div class="d-flex w-100 justify-content-between">
          <h5 class="mb-1">{{i.name}}</h5>
        </div>
        <p class="mb-1">{{i.description}}</p>
        <small class="text-body-secondary">Round and thin.</small>
      </a>
    </div>
  {{/each}}
{{/each}}
